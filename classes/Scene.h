#ifndef _SCENE__
#define _SCENE__
#include <SDL2/SDL.h>
/*  class Scene{
    private:
    public:
        virtual void draw(SDL_Renderer*) = 0;
        virtual ~Scene() = 0;
}; */
#endif

/* ifndef _SCENE__
class Scene{
    public:
        Scene();
}; */
